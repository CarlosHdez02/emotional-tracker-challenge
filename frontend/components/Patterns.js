/*import React from "react";
import { IdentifyContext } from "../context/IdentifyContext";


export const IdentifyPatterns = ()=>{

    const {emotions, loading, error, getEmotions} = React.useContext(IdentifyContext)   //  getting the emotions

    //  i have my emotions
    //  my function
    // need to get my emotions on first render

    React.useEffect(()=>{
        getEmotions()
    },[])


    return(
        <>
       
        
        
        </>
    )
}
*/